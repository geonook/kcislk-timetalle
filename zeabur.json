{
  "$schema": "https://zeabur.com/schema.json",
  "name": "kcislk-timetable-frontend-staging",
  "build": {
    "rootDirectory": "frontend",
    "buildCommand": "npm run build:staging",
    "outputDirectory": "dist"
  },
  "deploy": {
    "framework": "vite",
    "installCommand": "npm ci --legacy-peer-deps"
  },
  "environment": {
    "NODE_VERSION": "20",
    "VITE_ENABLE_EXAM_PROCTOR": "true"
  },
  "_comment": "此配置使用 Vite 靜態托管模式部署，不使用 Dockerfile。Frontend 透過 CORS 直接呼叫 Backend API (https://kcislk-backend.zeabur.app/api)。Dockerfile.frontend.docker 僅用於本地 Docker Compose 開發環境。"
}
